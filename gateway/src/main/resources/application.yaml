server:
  port: 9000
spring:
  application:
    name: gate-way
  cloud:
    nacos:
      username: nacos
      password: nacos
      discovery:
        server-addr: 127.0.0.1:8848
        namespace: public
  config:
    import:
      - optional:nacos:${spring.application.name}.${file-extension:yaml}
      - optional:nacos:common.${file-extension:yaml}

#management:
#  endpoints:
#    web:
#      exposure:
#        include: *